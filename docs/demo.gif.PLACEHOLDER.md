# Demo GIF placeholder

The README references `docs/demo.gif`. Record a short screencast (CLI run or the
Colab notebook end-to-end on `messy_example.xlsx`) and save it here as
`demo.gif` to replace this placeholder.

Suggested 10–15s storyboard:
1. `gene-tidy messy_example.xlsx --out outputs/`
2. Show the printed summary (clean / ambiguous / failed counts).
3. Open `clean_table.xlsx` and `ambiguous_rows.csv` to show `2-Sep → SEPTIN2`
   recovered and `1-Mar` flagged ambiguous.
